import { ChangeJwtInterceptor } from './change-jwt.interceptor';

describe('ChangeJwtInterceptor', () => {
  it('should be defined', () => {
    expect(new ChangeJwtInterceptor()).toBeDefined();
  });
});
