import { AddUserIdToBodyInterceptor } from './add-user-id-to-body.interceptor';

describe('AddUserIdToBodyInterceptor', () => {
  it('should be defined', () => {
    expect(new AddUserIdToBodyInterceptor()).toBeDefined();
  });
});
