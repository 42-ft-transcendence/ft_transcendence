import { TwoFactorInterceptor } from './two-factor.interceptor';

describe('TwoFactorInterceptor', () => {
  it('should be defined', () => {
    expect(new TwoFactorInterceptor()).toBeDefined();
  });
});
