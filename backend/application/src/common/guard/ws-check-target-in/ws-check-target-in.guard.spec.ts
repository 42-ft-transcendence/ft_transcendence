import { WsCheckTargetInGuard } from './ws-check-target-in.guard';

describe('WsCheckTargetInGuard', () => {
  it('should be defined', () => {
    expect(new WsCheckTargetInGuard()).toBeDefined();
  });
});
