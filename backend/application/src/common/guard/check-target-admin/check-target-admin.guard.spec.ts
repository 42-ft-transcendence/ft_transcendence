import { CheckTargetAdminGuard } from './check-target-admin.guard';

describe('CheckTargetAdminGuard', () => {
  it('should be defined', () => {
    expect(new CheckTargetAdminGuard()).toBeDefined();
  });
});
