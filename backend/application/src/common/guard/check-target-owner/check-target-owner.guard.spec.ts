import { CheckTargetOwnerGuard } from './check-target-owner.guard';

describe('CheckTargetOwnerGuard', () => {
  it('should be defined', () => {
    expect(new CheckTargetOwnerGuard()).toBeDefined();
  });
});
