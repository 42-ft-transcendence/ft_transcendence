import { ProcessChannelTypePipe } from './process-channel-type.pipe';

describe('ProcessChannelTypePipe', () => {
  it('should be defined', () => {
    expect(new ProcessChannelTypePipe()).toBeDefined();
  });
});
