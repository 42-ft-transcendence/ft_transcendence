import { HashPasswordPipePipe } from './hash-password.pipe';

describe('HashPasswordPipePipe', () => {
  it('should be defined', () => {
    expect(new HashPasswordPipePipe()).toBeDefined();
  });
});
