import { PrismaInvalidExceptionFilter } from './prisma-invalid-exception.filter';

describe('PrismaInvalidExceptionFilter', () => {
  it('should be defined', () => {
    expect(new PrismaInvalidExceptionFilter()).toBeDefined();
  });
});
