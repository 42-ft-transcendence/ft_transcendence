import { TwoFactorExceptionFilter } from './two-factor-exception.filter';

describe('TwoFactorExceptionFilter', () => {
  it('should be defined', () => {
    expect(new TwoFactorExceptionFilter()).toBeDefined();
  });
});
