import { SocketExceptionFilter } from './socket-exception.filter';

describe('SocketExceptionFilter', () => {
  it('should be defined', () => {
    expect(new SocketExceptionFilter()).toBeDefined();
  });
});
